<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Have your say...",:id => "comment_area" %>
  </div>
  <%= f.submit "Leave a Comment", class: "btn btn-primary" %>
<% end %>